# Sending and receiving Multipart

In the Spring backend, `MultipartController` is of interest.

In the Angular frontend, the `HttpClient` is not used due to limitations.
`MultiService` uses `fetch` instead.